# Nightly Runs

This directory is for local Codex nightly sprint run records.

Real run JSON files are local diagnostic artifacts and are ignored by default.

Allowed tracked files:

1. `README.md`
2. `.gitignore`

Do not commit real nightly run JSON, report payloads, review records, secrets, or production data.
