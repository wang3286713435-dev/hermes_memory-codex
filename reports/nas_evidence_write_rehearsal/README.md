# NAS Evidence Write Rehearsal Reports

This directory is for ignored local `nas_evidence_write_rehearsal.v0` artifacts.

These reports are temp-repository rehearsal artifacts only. They are not production evidence, do not authorize real writes, and must not be used as Agent answer citations.

Real report JSON and temp SQLite files are ignored by default.
