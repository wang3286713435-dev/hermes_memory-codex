# NAS Evidence Write Dry-run Reports

This directory is reserved for local `nas_evidence_write_dry_run.v0` reports.

Real report JSON files are local operator artifacts and are ignored by Git.

These reports are not production evidence, are not Agent citations, and do not authorize writes to Hermes DB, platform DB, OpenSearch, Qdrant, MinIO, NAS, or any document/chunk store.
