# Agent Runs

This directory stores local Codex run status files.

`latest.json` is intentionally ignored and should not be committed. It may contain transient phase status, local validation notes, and agent handoff metadata.

Commit only this README and ignore policy unless a future phase explicitly defines a sanitized template.
