"""Hermes built-in memory kernel.

The memory kernel is a core Hermes layer, not an external plugin. It owns query
routing, retrieval orchestration, context building, citation generation,
governance hooks, and memory writeback for agent requests.
"""

